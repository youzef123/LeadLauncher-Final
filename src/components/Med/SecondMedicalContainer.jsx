import styles from './SecondMedicalContainer.module.css';

export default function SecondMedicalContainer() {
    return (
        <section className={styles.container}>
            <h3 className={styles.missionText}>
                These details though are suseptible to changes so we see to it that every contact in our database is updated periodically.
                We can align our profiling capability depending on your data needs. Additional criteria that entail profiling through calling can also be negotiated.

<br></br><br></br>Our Data verification process includes verifying of phone numbers, email testing and multi-site online verification.
            </h3>
        </section>
    );
}